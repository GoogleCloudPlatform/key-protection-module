version: v2
modules:
  - path: .
    excludes:
      # Generated separately because its pinned external protos are not Buf modules.
      - keymanager/attestation_service/proto
deps:
  - buf.build/bufbuild/protovalidate
lint:
  except:
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - WIRE_JSON
